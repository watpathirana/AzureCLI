# AzureCLI
PowerShell command centers for Microsoft 365 &amp; Azure. Azure CLI.ps1 signs in via device code and runs az commands (VMs, storage, networking) from an interactive menu. graph email.ps1 signs into Microsoft Graph once and manages mail, calendar, files, Teams, and more. Sessions saved locally for reuse.
